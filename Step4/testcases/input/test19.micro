PROGRAM sqrt
BEGIN

	STRING dummy := "abcde";  --This is dummy to satisfy the grammar

     INT i, j, k;
     
	FUNCTION VOID main()
	BEGIN
		i := 0;
		j := 0;
		k := 0;
		result := 0;
		
		REPEAT
		     i := i + 1;
		     REPEAT
		          j := j + 1;
		          REPEAT
		               k := k + 1;
		               result := result + 1;
		          UNTIL (k > 99);
		     UNTIL (j > 99);
		UNTIL (i > 99);

		WRITE (result);
	END
END
