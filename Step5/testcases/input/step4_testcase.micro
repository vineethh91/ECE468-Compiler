PROGRAM step4
BEGIN

	INT a, b, i, p;

	FUNCTION VOID main()
	BEGIN

		i := 0;
		a := 0;
		b := 0;

		REPEAT
			READ (p);
			
			IF (p > 10) 
				THEN a := a + 1;
				ELSE b := b + 1;
			ENDIF

			i := i+1;

		UNTIL (i = 10);

		WRITE (a);
		WRITE (b);
	END
END
