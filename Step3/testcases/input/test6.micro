PROGRAM test
BEGIN
	INT a,b,c,x,y,z,h,j,k;
	FUNCTION VOID printout (INT a,INT b,INT c)
	BEGIN
		WRITE (a);
		WRITE (b);
		WRITE (c);
	END
  FUNCTION INT main()
	BEGIN
	IF (a = b)
	THEN
		
		h:=0;
	ELSE

	REPEAT
		a := j;
		j := j + 1;		
	UNTIL(j>10);

	ENDIF
	RETURN a+b;
	END
END
